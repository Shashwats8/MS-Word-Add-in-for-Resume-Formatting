{
  "name": "ms-word-resume-formatter-addin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx office-addin-dev-certs install && npx http-server ./ -p 3000 -S -C ./cert.pem -K ./key.pem",
    "clean-certs": "npx office-addin-dev-certs uninstall"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "office-addin-dev-certs": "^3.2.0"
  }
}
